import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    title: {
        fontSize: 64,
        fontWeight: 'bold',
        textAlign: 'center',
        color: '#000',
        marginBottom: 4,
        marginTop: 10,
    },
    titleDark: {
        fontSize: 64,
        fontWeight: 'bold',
        textAlign: 'center',
        color: '#fff',
    },
    subtitle: {
        fontWeight: 'bold',
        fontSize: 16,
        marginBottom: 8,
        color: '#000'
    },
    subtitleDark:{
        color: '#fff'
    }
})